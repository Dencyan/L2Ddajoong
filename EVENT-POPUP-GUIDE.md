# Dajoong 팝업 할인 이벤트 관리

현재 이벤트: 팝업 문의 시 의뢰 총액에서 50,000원 할인. 기존 할인과 중복 불가. 종료일 없음.

## 종료하는 가장 간단한 방법

사이트 루트 `event-promotion.js` 상단의 `EVENT_CONFIG`에서 `enabled: true`를 `enabled: false`로 바꾸고 운영 사이트에 배포합니다.

이 설정은 새로 불러온 페이지에서 다음을 함께 끕니다.

- 자동 팝업과 이벤트 다시 열기 버튼
- 메인/가격표의 이벤트 할인 금액과 메인 이벤트 안내
- 미니 견적의 팝업 할인 선택지
- 문의 폼의 이벤트 안내와 `promotion_*` 숨김 필드

기본 가격과 기존 할인은 유지됩니다. 기존에 열려 있는 페이지는 새로고침해야 변경 설정을 받습니다.

## 자동 종료일을 지정하는 방법

```js
const EVENT_CONFIG = {
  enabled: true,
  endsAt: '2026-12-31T23:59:59+09:00',
};
```

위 날짜는 형식 예시입니다. 실제 종료일로 바꾸세요. `+09:00`은 한국 시간입니다. `null`은 종료일 없음입니다. 잘못된 날짜를 넣으면 이벤트가 표시되지 않습니다. 팝업 하단의 긴 조건 문구는 표시하지 않습니다. 종료 설정과 중복 할인 방지 규칙은 유지됩니다. 적용 판단은 페이지를 불러오는 시점의 방문자 기기 시간을 사용합니다.

## 코드에서 찾거나 완전히 제거하기

전체 검색으로 `DAJOONG_POPUP_EVENT`를 찾으세요. 21개 운영 페이지에서 아래 구간을 표시했습니다.

```html
<!-- DAJOONG_POPUP_EVENT START: 종료 설정은 event-promotion.js의 EVENT_CONFIG -->
<script src="/event-promotion.js"></script>
<!-- DAJOONG_POPUP_EVENT END -->
```

파일까지 제거하려면 모든 페이지의 위 구간을 먼저 제거한 다음 `event-promotion.js`를 제거하세요. 스크립트만 먼저 지우면 페이지가 없는 파일을 요청합니다. 이벤트용 스타일과 `data-event-base-price` 속성이 남아 있어도 원래 가격은 유지됩니다. 기본 기능의 코드를 지우지 않으려면 파일 삭제보다 `enabled: false`가 간단합니다.

## 팝업 표시 방식

- 방문·새로고침·다른 페이지 이동 시 자동 표시됩니다. 팝업 문의 버튼으로 이동한 접수 화면은 입력을 방해하지 않도록 제외됩니다.
- 닫기 또는 Esc는 현재 화면만 닫습니다. 다음 방문에는 다시 표시됩니다.
- ‘오늘 하루 열지 않기’는 한국 시간 자정까지 숨깁니다. 같은 브라우저의 탭과 언어 페이지에서 공유됩니다. 자정 이후 다음 페이지 로드 시 다시 표시됩니다.
- 숨김 키는 localStorage의 `popup-inquiry-50000:hidden-until`입니다. 브라우저 저장이 차단되면 안내를 표시하며, 일반 닫기는 계속 사용할 수 있습니다.
- 기존 세션의 dismissed/claimed 값 때문에 팝업이 자동으로 숨겨지지 않습니다. 할인 선택 정보는 별도로 유지합니다.
- 화면 아래 이벤트 버튼으로 다시 열 수 있습니다.
- 팝업 문의 버튼은 해당 언어의 문의 페이지로 이동합니다.
- 이벤트 문의 여부는 URL과 탭 세션으로 이어지며, 문의 폼에 이벤트 정보가 함께 구성됩니다.
- 실제 문의 전송과 이메일 도착 확인은 별도 검증이 필요합니다.

## 이번 확인 범위

2026-09-12 로컬 수정본에서 21개 페이지를 1280px/360px 너비로 확인했습니다. 세 언어 갤러리에서 미리보기 포함 21개 이미지씩 로딩됐고, 언어 전환·견적 수량 제한·할인 중복 방지·팝업 열기/닫기/문의 이동을 확인했습니다. 이벤트 비활성 및 만료 설정에서 팝업·할인가·견적 이벤트·문의 태그가 사라지는 것도 별도 테스트 페이지로 확인했습니다. 운영 사이트 배포와 실제 문의 전송은 하지 않았습니다.

## 배포 시 캐시 갱신

CSS와 JavaScript 참조의 `?v=`는 파일 버전입니다. 이벤트 설정 또는 디자인을 수정하면 해당 파일을 참조하는 모든 HTML의 버전도 새 값으로 바꾸어 함께 커밋·배포하세요. 이전 캐시 때문에 종료 설정이 늦게 반영되는 것을 방지합니다.
